[workspace]

members = [
    "lua-dom",
    "lua-pool",
    "lua-shell",
    "lua-fs",
    "lua-json",
    "lua-util",
    "lua-core",
    "lua-http",
    "lua-date",
    "lua-config",
    "lua-cli",
]


[profile.release]
strip = "symbols"
lto = "fat"
